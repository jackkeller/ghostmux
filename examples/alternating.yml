windows:
  - name: alternating
    root: ~/code/my-project
    panes:
      - commands:
          - echo "Left"
      - commands:
          - echo "Top Right"
      - commands:
          - echo "Bottom Right"
